import { LmsDirectDirective } from './lms-direct.directive';

describe('LmsDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new LmsDirectDirective();
    expect(directive).toBeTruthy();
  });
});
